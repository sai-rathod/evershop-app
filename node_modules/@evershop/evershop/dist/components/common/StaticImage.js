import { useAppState } from '@components/common/context/app.js';
import { Image } from '@components/common/Image.js';
import React, { useMemo } from 'react';
export const StaticImage = ({ subPath, quality = 75, ...props }) => {
    var _a;
    const { graphqlResponse } = useAppState();
    const baseUrl = ((_a = graphqlResponse === null || graphqlResponse === void 0 ? void 0 : graphqlResponse.pageMeta) === null || _a === void 0 ? void 0 : _a.baseUrl) || '';
    const imagePath = useMemo(() => {
        const formattedSubPath = subPath.startsWith('/')
            ? subPath.substring(1)
            : subPath;
        return `${baseUrl}/assets/${formattedSubPath}`;
    }, [baseUrl, subPath]);
    return React.createElement(Image, { src: imagePath, quality: quality, ...props });
};
//# sourceMappingURL=StaticImage.js.map