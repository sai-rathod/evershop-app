import React from 'react';
interface RenderIfTrueProps {
    condition: boolean;
    children: React.ReactNode;
}
export default function RenderIfTrue({ condition, children }: RenderIfTrueProps): React.ReactNode;
export {};
