import React from 'react';
import { RegisterOptions, FieldPath, FieldValues } from 'react-hook-form';
interface TelFieldProps<T extends FieldValues = FieldValues> extends Omit<React.InputHTMLAttributes<HTMLInputElement>, 'name' | 'type'> {
    name: FieldPath<T>;
    label?: string;
    error?: string;
    helperText?: string;
    required?: boolean;
    validation?: RegisterOptions<T>;
    wrapperClassName?: string;
    prefixIcon?: React.ReactNode;
    suffixIcon?: React.ReactNode;
}
export declare function TelField<T extends FieldValues = FieldValues>({ name, label, error, wrapperClassName, helperText, required, validation, className, prefixIcon, suffixIcon, ...props }: TelFieldProps<T>): React.JSX.Element;
export {};
