import React from 'react';
import { RegisterOptions } from 'react-hook-form';
interface PasswordFieldProps {
    name: string;
    label?: string;
    placeholder?: string;
    className?: string;
    required?: boolean;
    disabled?: boolean;
    minLength?: number;
    showToggle?: boolean;
    error?: string;
    helperText?: string;
    validation?: RegisterOptions;
    onChange?: (value: string) => void;
    wrapperClassName?: string;
    prefixIcon?: React.ReactNode;
    suffixIcon?: React.ReactNode;
}
export declare function PasswordField({ name, label, placeholder, className, wrapperClassName, required, disabled, minLength, showToggle, error, helperText, validation, onChange, prefixIcon, suffixIcon, ...props }: PasswordFieldProps): React.JSX.Element;
export {};
