import React from 'react';
import { RegisterOptions } from 'react-hook-form';
interface EmailFieldProps {
    name: string;
    label?: string;
    placeholder?: string;
    className?: string;
    required?: boolean;
    disabled?: boolean;
    defaultValue?: string;
    error?: string;
    helperText?: string;
    validation?: RegisterOptions;
    onChange?: (value: string) => void;
    wrapperClassName?: string;
    prefixIcon?: React.ReactNode;
    suffixIcon?: React.ReactNode;
}
export declare function EmailField({ name, label, placeholder, className, wrapperClassName, required, disabled, defaultValue, error, helperText, validation, onChange, prefixIcon, suffixIcon, ...props }: EmailFieldProps): React.JSX.Element;
export {};
