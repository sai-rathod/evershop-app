import React from 'react';
import { RegisterOptions, FieldPath, FieldValues } from 'react-hook-form';
interface TimeFieldProps<T extends FieldValues = FieldValues> extends Omit<React.InputHTMLAttributes<HTMLInputElement>, 'name' | 'type'> {
    name: FieldPath<T>;
    label?: string;
    error?: string;
    helperText?: string;
    required?: boolean;
    validation?: RegisterOptions<T>;
    wrapperClassName?: string;
}
export declare function TimeField<T extends FieldValues = FieldValues>({ name, label, error, wrapperClassName, helperText, required, validation, className, min, max, step, ...props }: TimeFieldProps<T>): React.JSX.Element;
export {};
