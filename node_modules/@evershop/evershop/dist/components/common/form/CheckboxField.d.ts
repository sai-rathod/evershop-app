import React from 'react';
import { RegisterOptions, FieldPath, FieldValues } from 'react-hook-form';
interface CheckboxOption {
    value: string | number;
    label: string;
    disabled?: boolean;
}
interface CheckboxFieldProps<T extends FieldValues = FieldValues> extends Omit<React.InputHTMLAttributes<HTMLInputElement>, 'name' | 'type' | 'defaultValue'> {
    name: FieldPath<T>;
    label?: string;
    error?: string;
    helperText?: string;
    required?: boolean;
    validation?: RegisterOptions<T>;
    options?: CheckboxOption[];
    defaultValue?: boolean | (string | number)[];
    direction?: 'horizontal' | 'vertical';
    wrapperClassName?: string;
}
export declare function CheckboxField<T extends FieldValues = FieldValues>({ name, label, error, wrapperClassName, helperText, required, validation, options, defaultValue, direction, className, disabled, ...props }: CheckboxFieldProps<T>): React.JSX.Element;
export {};
