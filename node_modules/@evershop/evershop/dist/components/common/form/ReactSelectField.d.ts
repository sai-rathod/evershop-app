import React from 'react';
import { FieldPath, FieldValues, RegisterOptions } from 'react-hook-form';
import { Props as ReactSelectProps } from 'react-select';
interface SelectOption {
    value: any;
    label: string;
    [key: string]: any;
}
interface ReactSelectFieldProps<T extends FieldValues = FieldValues> extends Omit<ReactSelectProps<SelectOption>, 'name' | 'value' | 'onChange'> {
    name: FieldPath<T>;
    label?: string;
    error?: string;
    helperText?: string;
    required?: boolean;
    validation?: RegisterOptions<T>;
    options: SelectOption[];
    className?: string;
    wrapperClassName?: string;
    defaultValue?: any;
}
export declare function ReactSelectField<T extends FieldValues = FieldValues>({ name, label, error, wrapperClassName, helperText, required, validation, options, className, isMulti, defaultValue, ...selectProps }: ReactSelectFieldProps<T>): React.JSX.Element;
export {};
