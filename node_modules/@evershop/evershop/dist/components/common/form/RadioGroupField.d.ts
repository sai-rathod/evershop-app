import React from 'react';
import { RegisterOptions, FieldPath, FieldValues } from 'react-hook-form';
interface RadioOption {
    value: string | number;
    label: string;
    disabled?: boolean;
}
interface RadioGroupFieldProps<T extends FieldValues = FieldValues> extends Omit<React.InputHTMLAttributes<HTMLInputElement>, 'name' | 'type' | 'value' | 'checked' | 'onChange' | 'onBlur'> {
    name: FieldPath<T>;
    options: RadioOption[];
    label?: string;
    error?: string;
    helperText?: string;
    direction?: 'horizontal' | 'vertical';
    required?: boolean;
    disabled?: boolean;
    validation?: RegisterOptions<T>;
    defaultValue?: string | number;
    wrapperClassName?: string;
}
export declare function RadioGroupField<T extends FieldValues = FieldValues>({ name, options, label, error, wrapperClassName, helperText, className, direction, required, disabled, validation, defaultValue, ...props }: RadioGroupFieldProps<T>): React.JSX.Element;
export {};
