import React from 'react';
import { FieldPath, FieldValues, RegisterOptions } from 'react-hook-form';
import { CreatableProps } from 'react-select/creatable';
interface SelectOption {
    value: any;
    label: string;
    [key: string]: any;
}
interface ReactSelectCreatableFieldProps<T extends FieldValues = FieldValues> extends Omit<CreatableProps<SelectOption, boolean, any>, 'name' | 'value' | 'onChange'> {
    name: FieldPath<T>;
    label?: string;
    error?: string;
    helperText?: string;
    required?: boolean;
    validation?: RegisterOptions<T>;
    options: SelectOption[];
    className?: string;
    wrapperClassName?: string;
    defaultValue?: any;
    onCreateOption?: (inputValue: string) => void;
    formatCreateLabel?: (inputValue: string) => string;
}
export declare function ReactSelectCreatableField<T extends FieldValues = FieldValues>({ name, label, error, wrapperClassName, helperText, required, validation, options, className, isMulti, defaultValue, onCreateOption, formatCreateLabel, ...selectProps }: ReactSelectCreatableFieldProps<T>): React.JSX.Element;
export {};
