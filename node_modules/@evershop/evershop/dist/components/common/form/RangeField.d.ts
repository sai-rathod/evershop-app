import React from 'react';
import { RegisterOptions, FieldPath, FieldValues } from 'react-hook-form';
interface RangeFieldProps<T extends FieldValues = FieldValues> extends Omit<React.InputHTMLAttributes<HTMLInputElement>, 'name' | 'type'> {
    name: FieldPath<T>;
    label?: string;
    error?: string;
    helperText?: string;
    required?: boolean;
    validation?: RegisterOptions<T>;
    showValue?: boolean;
    defaultValue?: number;
    wrapperClassName?: string;
}
export declare function RangeField<T extends FieldValues = FieldValues>({ name, label, error, wrapperClassName, helperText, required, validation, showValue, defaultValue, className, min, max, step, ...props }: RangeFieldProps<T>): React.JSX.Element;
export {};
