import React from 'react';
import './CategoryTree.scss';
export interface CategoryTreeItem {
    categoryId: number;
    name: string;
    hasChildren: boolean;
    path: Array<{
        name: string;
    }>;
    children?: Array<CategoryTreeItem>;
}
export interface CategoryItemProps {
    category: CategoryTreeItem;
    selectedCategories?: CategoryTreeItem[];
    onSelect: (category: CategoryTreeItem) => void;
}
interface CategoryTreeProps {
    selectedCategories?: CategoryTreeItem[];
    onSelect: (category: CategoryTreeItem) => void;
}
declare function CategoryTree({ selectedCategories, onSelect }: CategoryTreeProps): React.JSX.Element;
declare namespace CategoryTree {
    var defaultProps: {
        selectedCategories: never[];
    };
}
export { CategoryTree };
