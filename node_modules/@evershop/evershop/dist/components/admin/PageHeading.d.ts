import React from 'react';
import './PageHeading.scss';
export interface PageHeadingProps {
    backUrl?: string;
    heading: string;
}
declare function PageHeading({ backUrl, heading }: PageHeadingProps): React.JSX.Element | null;
declare namespace PageHeading {
    var defaultProps: {
        backUrl: undefined;
    };
}
export { PageHeading };
