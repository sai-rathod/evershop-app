import React from 'react';
import './Pagination.scss';
export interface PaginationProps {
    total: number;
    limit: number;
    page: number;
}
export declare function Pagination({ total, limit, page }: PaginationProps): React.JSX.Element;
