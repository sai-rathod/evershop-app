import React from 'react';
import './Filter.scss';
export interface FilterProps {
    title: string;
    options: Array<{
        label: string;
        value: string;
        onSelect: () => void;
    }>;
    selectedOption?: string;
}
export declare function Filter({ title, options, selectedOption }: FilterProps): React.JSX.Element;
export declare namespace Filter {
    var defaultProps: {
        title: string;
        options: never[];
        selectedOption: string;
    };
}
