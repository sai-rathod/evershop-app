import React from 'react';
export interface SortableHeaderProps {
    title: string;
    name: string;
    currentFilters: Array<{
        key: string;
        value: string;
    }>;
}
export declare function SortableHeader({ title, name, currentFilters }: SortableHeaderProps): React.JSX.Element;
export declare namespace SortableHeader {
    var defaultProps: {
        currentFilters: never[];
    };
}
