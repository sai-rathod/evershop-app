import React from 'react';
import './Card.scss';
export interface CardProps {
    title?: string | React.ReactNode;
    actions?: Array<{
        onAction?: () => void;
        variant?: 'primary' | 'secondary' | 'critical' | 'interactive';
        name: string;
    }>;
    subdued?: boolean;
    children: React.ReactNode;
}
declare function Card({ title, actions, subdued, children }: CardProps): React.JSX.Element;
declare namespace Card {
    var defaultProps: {
        actions: never[];
        subdued: boolean;
        title: string;
    };
    var Session: {
        ({ actions, title, children }: CardSessionProps): React.JSX.Element;
        defaultProps: {
            actions: never[];
            title: string;
            children: null;
        };
    };
}
export interface CardSessionProps {
    actions?: Array<{
        onAction?: () => void;
        variant?: 'primary' | 'secondary' | 'critical' | 'interactive';
        name: string;
    }>;
    title?: string | React.ReactNode;
    children: React.ReactNode;
}
export { Card };
