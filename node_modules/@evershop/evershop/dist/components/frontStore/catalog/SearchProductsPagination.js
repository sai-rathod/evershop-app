import { Area } from '@components/common/index.js';
import { useSearch } from '@components/frontStore/catalog/SearchContext.js';
import { Pagination, DefaultPaginationRenderer } from '@components/frontStore/Pagination.js';
import React from 'react';
export function SearchProductsPagination() {
    const { products } = useSearch();
    const page = products.currentFilters.find((filter) => filter.key === 'page');
    const limit = products.currentFilters.find((filter) => filter.key === 'limit');
    return (React.createElement(React.Fragment, null,
        React.createElement(Area, { id: "searchProductsPaginationBefore", noOuter: true }),
        React.createElement(Pagination, { total: products.total, limit: limit ? parseInt(limit.value, 10) : 20, currentPage: parseInt((page === null || page === void 0 ? void 0 : page.value) || '1', 10) }, (paginationProps) => (React.createElement(DefaultPaginationRenderer, { renderProps: paginationProps }))),
        React.createElement(Area, { id: "searchProductsPaginationAfter", noOuter: true })));
}
//# sourceMappingURL=SearchProductsPagination.js.map