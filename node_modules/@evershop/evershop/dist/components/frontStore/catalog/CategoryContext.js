import React, { createContext, useContext } from 'react';
const CategoryContext = createContext(undefined);
export const CategoryProvider = ({ children, category }) => {
    return (React.createElement(CategoryContext.Provider, { value: category }, children));
};
export const useCategory = () => {
    const context = useContext(CategoryContext);
    if (context === undefined) {
        throw new Error('useCategory must be used within a CategoryProvider');
    }
    return context;
};
//# sourceMappingURL=CategoryContext.js.map