import React from 'react';
interface PaymentMethod {
    code: string;
    name: string;
    cost?: {
        value: number;
        text: string;
    };
    description?: string;
}
export declare function PaymentMethods({ methods, isLoading }: {
    methods: PaymentMethod[];
    isLoading?: boolean;
}): React.JSX.Element;
export {};
