import { CustomerAddressGraphql } from '@evershop/evershop/types/customerAddress';
import React from 'react';
interface CustomerAddressFormProps {
    allowCountries: {
        value: string;
        label: string;
        provinces: {
            value: string;
            label: string;
        }[];
    }[];
    address?: CustomerAddressGraphql;
    areaId?: string;
    fieldNamePrefix?: string;
}
export declare function CustomerAddressForm({ allowCountries, address, areaId, fieldNamePrefix }: CustomerAddressFormProps): React.JSX.Element;
export {};
