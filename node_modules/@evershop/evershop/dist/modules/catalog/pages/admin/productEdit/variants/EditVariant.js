import { Modal } from '@components/common/modal/Modal.js';
import { useModal } from '@components/common/modal/useModal.js';
import React from 'react';
import { VariantModal } from './VariantModal.js';
export const EditVariant = ({ variant, refresh, variantGroup }) => {
    const modal = useModal({
        onAfterClose: () => {
            refresh();
        }
    });
    return (React.createElement("div", null,
        React.createElement("a", { className: "button", onClick: (e) => {
                e.preventDefault();
                modal.open();
            }, href: "#" }, "Edit"),
        React.createElement(Modal, { isOpen: modal.isOpen, onClose: modal.close },
            React.createElement(VariantModal, { variant: variant, variantGroup: variantGroup, closeModal: modal.close }))));
};
//# sourceMappingURL=EditVariant.js.map