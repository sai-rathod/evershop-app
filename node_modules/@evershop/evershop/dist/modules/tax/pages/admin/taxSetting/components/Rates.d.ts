import React from 'react';
import { TaxRate } from './Rate.js';
interface RatesProps {
    getTaxClasses: (options?: {
        requestPolicy?: string;
    }) => Promise<void> | void;
    rates: Array<TaxRate>;
    addRateApi: string;
}
export declare function Rates({ getTaxClasses, rates, addRateApi }: RatesProps): React.JSX.Element;
export {};
