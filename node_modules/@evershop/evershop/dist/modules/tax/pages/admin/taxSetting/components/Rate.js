import { Modal } from '@components/common/modal/Modal.js';
import { useModal } from '@components/common/modal/useModal.js';
import React from 'react';
import { RateForm } from './RateForm.js';
function Rate({ rate, getTaxClasses }) {
    const modal = useModal();
    return (React.createElement(React.Fragment, null,
        React.createElement(React.Fragment, null,
            React.createElement("td", { className: "border-none py-2" }, rate.name),
            React.createElement("td", { className: "border-none py-2" },
                rate.rate,
                "%"),
            React.createElement("td", { className: "border-none py-2" }, rate.isCompound ? 'Yes' : 'No'),
            React.createElement("td", { className: "border-none py-2" }, rate.priority),
            React.createElement("td", { className: "border-none py-2" },
                React.createElement("a", { href: "#", className: "text-interactive", onClick: (e) => {
                        e.preventDefault();
                        modal.open();
                    } }, "Edit"),
                React.createElement("a", { href: "#", className: "text-critical ml-5", onClick: async (e) => {
                        e.preventDefault();
                        await fetch(rate.deleteApi, {
                            method: 'DELETE'
                        });
                        await getTaxClasses({ requestPolicy: 'network-only' });
                    } }, "Delete"))),
        React.createElement(Modal, { title: "Edit tax rate", onClose: modal.close, isOpen: modal.isOpen },
            React.createElement(RateForm, { saveRateApi: rate.updateApi, closeModal: () => modal.close(), getTaxClasses: getTaxClasses, rate: rate }))));
}
export { Rate };
//# sourceMappingURL=Rate.js.map