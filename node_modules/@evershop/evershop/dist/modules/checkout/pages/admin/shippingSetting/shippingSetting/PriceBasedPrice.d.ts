import React from 'react';
export interface PriceBasedPriceProps {
    lines: Array<{
        minPrice: {
            value: number;
        };
        cost: {
            value: number;
        };
    }>;
}
export declare function PriceBasedPrice({ lines }: PriceBasedPriceProps): React.JSX.Element;
