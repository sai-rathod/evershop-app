import React from 'react';
import { Operator } from './conditionCriterias.js';
export interface RequiredProduct {
    key: string;
    operator: Operator;
    value: string | number | Array<string> | Array<number>;
    qty: string;
    editable?: boolean;
}
export interface RequiredProductsProps {
    requiredProducts: Array<RequiredProduct>;
}
export declare function RequiredProducts({ requiredProducts }: RequiredProductsProps): React.JSX.Element;
export declare namespace RequiredProducts {
    var defaultProps: {
        requiredProducts: never[];
    };
}
