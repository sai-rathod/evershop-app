import { AttributeGroupSelector } from '@components/admin/AttributeGroupSelector.js';
import { Modal } from '@components/common/modal/Modal.js';
import { useModal } from '@components/common/modal/useModal.js';
import React from 'react';
export const AttributeGroupConditionValueSelector = ({ selectedValues, updateCondition, isMulti }) => {
    const skus = Array.isArray(selectedValues) ? selectedValues : [];
    const selectedIds = React.useRef(skus || []);
    const modal = useModal({
        onAfterClose: () => {
            updateCondition(selectedIds.current);
        }
    });
    const onSelect = (id) => {
        if (!isMulti) {
            selectedIds.current = [id];
            modal.close();
        }
        else {
            const prev = selectedIds.current;
            if (!prev.includes(id)) {
                selectedIds.current = [id, ...prev];
            }
        }
    };
    const onUnSelect = async (id) => {
        const prev = selectedIds.current;
        selectedIds.current = prev.filter((s) => s !== id);
    };
    return (React.createElement("div", null,
        React.createElement("a", { href: "#", className: "text-interactive hover:underline", onClick: (e) => {
                e.preventDefault();
                modal.open();
            } },
            selectedIds.current.map((id, index) => (React.createElement("span", { key: id },
                index === 0 && React.createElement("span", { className: "italic" },
                    "\u2018",
                    id,
                    "\u2019"),
                index === 1 && (React.createElement("span", null,
                    " and ",
                    selectedIds.current.length - 1,
                    " more"))))),
            selectedIds.current.length === 0 && (React.createElement("span", null, "Choose Attribute Groups"))),
        React.createElement(Modal, { title: "Select Attribute Groups", onClose: modal.close, isOpen: modal.isOpen },
            React.createElement("div", { className: "overflow-auto", style: { maxHeight: '60vh' } },
                React.createElement(AttributeGroupSelector, { onSelect: onSelect, onUnSelect: onUnSelect, selectedAttributeGroups: selectedIds.current.map((id) => ({
                        attributeGroupId: id,
                        uuid: undefined
                    })) })))));
};
//# sourceMappingURL=AttributeGroupConditionValueSelector.js.map