import React from 'react';
import { Operator } from './conditionCriterias.js';
export declare const PriceConditionValueSelector: ({ updateCondition, condition }: {
    updateCondition: (values: string | number | Array<string> | Array<number>) => void;
    condition: {
        key: string;
        operator: Operator;
        value: number;
    };
}) => React.JSX.Element;
