import React from 'react';
interface SeoMetaProps {
    pageInfo: {
        title: string;
        description: string;
    };
}
export default function SeoMeta({ pageInfo: { title, description } }: SeoMetaProps): React.JSX.Element;
export declare const layout: {
    areaId: string;
    sortOrder: number;
};
export declare const query = "\n  query query {\n    pageInfo {\n      title\n      description\n    }\n  }\n";
export {};
