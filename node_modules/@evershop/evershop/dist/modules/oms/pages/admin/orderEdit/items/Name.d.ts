import React from 'react';
interface NameProps {
    name: string;
    productSku: string;
    productUrl: string;
    variantOptions?: Array<{
        attributeName: string;
        optionText: string;
    }>;
}
export declare function Name({ name, productSku, productUrl, variantOptions }: NameProps): React.JSX.Element;
export {};
