interface ProcessedImage {
    buffer: Buffer;
    path: string;
    metadata: {
        format: string;
        width: number;
        height: number;
        contentType: string;
    };
}
export declare const imageProcessor: (src: string, width: number, quality: number, format?: "jpeg" | "png" | "webp" | "avif", height?: number, allowUpscale?: boolean) => Promise<ProcessedImage>;
export {};
