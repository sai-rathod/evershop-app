import React from 'react';
interface Props {
    captureAPI: string;
    order: {
        paymentStatus: {
            code: string;
        };
        uuid: string;
        paymentMethod: string;
    };
}
export default function PaypalCaptureButton({ captureAPI, order: { paymentStatus, uuid, paymentMethod } }: Props): React.JSX.Element;
export declare const layout: {
    areaId: string;
    sortOrder: number;
};
export declare const query = "\n  query Query {\n    captureAPI: url(routeId: \"paypalCaptureAuthorizedPayment\")\n    order(uuid: getContextValue(\"orderId\")) {\n      uuid\n      paymentStatus {\n        code\n      }\n      paymentMethod\n    }\n  }\n";
export {};
