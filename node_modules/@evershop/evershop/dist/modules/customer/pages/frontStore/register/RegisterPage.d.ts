import React from 'react';
interface RegisterPageProps {
    homeUrl: string;
    loginUrl: string;
}
export default function RegisterPage({ homeUrl, loginUrl }: RegisterPageProps): React.JSX.Element;
export declare const layout: {
    areaId: string;
    sortOrder: number;
};
export declare const query = "\n  query Query {\n    homeUrl: url(routeId: \"homepage\")\n    loginUrl: url(routeId: \"login\")\n  }\n";
export {};
