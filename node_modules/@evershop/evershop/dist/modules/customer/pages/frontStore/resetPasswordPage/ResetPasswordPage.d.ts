import React from 'react';
interface ResetPasswordFormProps {
    action: string;
}
export default function ResetPasswordPage({ action }: ResetPasswordFormProps): React.JSX.Element;
export declare const layout: {
    areaId: string;
    sortOrder: number;
};
export declare const query = "\n  query Query {\n    action: url(routeId: \"resetPassword\")\n  }\n";
export {};
